name: learn_mask_coauthor_cora

load_fn: 'train_results/coauthor_cora/allsettransformer/run1'
save_fn: 'train_results/coauthor_cora/allsettransformer/run1/explanation.json'
load_best: True
motif: null

node_samples: null
node_idxs: null
compute_complement: True
save_every: 25

num_expansions: 3

# evaluation
loss_pred_type: kl_div
coeffs:
  size: 0.005
  ent: 0.01

log_wandb: False

defaults:  
  - _self_  
  - expl_method: learn_mask
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  

hydra:  
  output_subdir: null  
  run:  
    dir: .
  